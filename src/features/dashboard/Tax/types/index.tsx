export interface TaxProps {
    initialTaxes: {
        
        // tax_id: number;
        // tax_type: string;
        // tax_name: string;
        // tax_value: number | null;
        // service_value: number;
        // tax_status: boolean;
        // created_at: string;
        // updated_at: string;
        // deleted_at: string | null;
        // supplier_supplier_id: number | null;
    };
    initialMeta: {
        currentPage: number;
        totalPages: number;
        totalItems: number;
    };
}
